@model EmployeeManagement.Domain.ViewModels.Department.EditDepartmentVM
@{
    ViewData["Title"] = "Edit Department";
}
<h2 class="text-center font-weight-bold">Edit Department</h2>

<div class="row align-items-center justify-content-center">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-body">
                <form asp-action="Edit" asp-controller="Department" method="post">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="row">
                        @Html.HiddenFor(c=>c.Id)
                        <div class="col-md-4 form-group">
                            <label asp-for="Name" class="lang control-label">Name</label>
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                        <div class="col-md-4 form-group">
                            <label asp-for="ManagerId" class="lang control-label">Manager</label>
                            <select asp-for="ManagerId" class="form-control" asp-items="@ViewBag.Employees">
                                <option value="@null">Select</option>
                            </select>
                        </div>
                    </div>

                    <div class="form-group row mb-4">
                        <div class="col-6 text-left">
                            <input type="submit" class="btn btn-primary font-weight-bold" value="Save" />
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@await Html.PartialAsync("~/Views/Shared/_common_message.cshtml")

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}